image: "ubuntu:20.04"

stages:
  - publish

🚀 publish package:
  stage: publish
  only: 
    - tags
  variables:
    CF_API_KEY: ${CF_API_KEY}
  script:
    - apt-get update
    - apt-get install -y curl zip jq git
    - git --version
    - cd .release
    - chmod +x release.sh
    - ./release.sh
