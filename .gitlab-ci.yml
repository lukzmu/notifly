stages:
  - publish

before_script:
  - apt-get install curl zip jq

🚀 publish package:
  stage: publish
  only: 
    - tags
  script:
    - cd .release
    - chmod +x release.sh
    - ./release.sh
