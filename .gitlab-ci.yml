image: "ubuntu:20.04"

stages:
  - publish

before_script:
  - apt-get update
  - apt-get install curl zip jq

🚀 publish package:
  stage: publish
  only: 
    - tags
  script:
    - cd .release
    - chmod +x release.sh
    - ./release.sh
